using System;
using System.Collections.Generic;
using System.Text;

namespace TraningSession
{
    class List_Session1
    {
        public List_Session1()
        {
            /*
            List<int> LobjNumbers1 = new List<int>();
            List<int> LobjNumbers2 = new List<int>();

            for (int i = 0; i <= 10; i++)
            {
                LobjNumbers1.Add(i);
            }

            for (int i = 11; i <= 20; i++)
            {
                LobjNumbers2.Add(i);
            }

            LobjNumbers1.AddRange(LobjNumbers2);

            foreach (var item in LobjNumbers1)
            {
                Console.WriteLine(string.Format("This is Test message for {0}", item));
            }
            */


            Dictionary<string, string> dictionaryObj = new Dictionary<string, string>();
            dictionaryObj.Add("Test1","1234");
            dictionaryObj.Add("Test2", "1234");


        }
    }
}
